import React from "react";
import ReactDOM from "react-dom/client";
import TrustCV from "./TrustCV.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrustCV />
  </React.StrictMode>
);